Decide whether this email should receive a reply from a human.

Analyze the message and metadata (subject, sender, headers, labels). 
If previous conversation turns are shown, consider them briefly to decide if the thread is already resolved or still awaiting a response.

Message metadata:
From: {from_}
To: {to}
Subject: {subject}
Date: {date}
Labels: {labels}

Current message:
{body}

Earlier messages (if any):
{previous_emails}

Output strictly the JSON object as specified in the system instructions.